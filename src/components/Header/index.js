// == Import npm
import React from 'react';

// == Import
import HeaderStyled from './HeaderStyled';

// == Composant
const Header = () => (
  <HeaderStyled>
    <nav>
      <a href="#">home</a>
      <a href="#">no limit</a>
      <a href="#">test</a>
    </nav>
  </HeaderStyled>
);

export default Header;
